# Export all names
exportPattern(".")

# Import all packages listed as Imports or Depends
import(
  sf,
  ncdf4,
  maps,
  mapdata
)

# Import functions na.omit() and dist() from "stats" package.
importFrom("stats", "dist", "na.omit")
